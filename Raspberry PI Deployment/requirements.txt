fastapi==0.115.6
uvicorn==0.29.0
python-multipart==0.0.9
ultralytics==8.4.37
click>=8.1.7
Pillow

# numpy, opencv, picamera2, libcamera are NOT listed here.
# They are provided by the Pi OS system packages and inherited via
# --system-site-packages. Installing them via pip causes version conflicts.
